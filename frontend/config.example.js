/*
 * Deployment configuration template.
 * Replace the placeholder URL when hosting the frontend separately from the API.
 */
window.SMART_EXPENSE_CONFIG = {
  API_BASE_URL: "https://your-backend-domain.com"
};
